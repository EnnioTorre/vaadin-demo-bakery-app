apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: grafana-route
  namespace: dev-rcp-1
  labels:
    name: grafana-route
    app: grafana-app
    project: dev-rcp-1
    operated-prometheus: "true"
spec:
  path: /
  to:
    kind: Service
    name: grafana-service
    weight: 100
  port:
    targetPort: grafana
  tls:
    termination: edge
  wildcardPolicy: None